\; ================================
; RCM GUI - Auto Generated
; ================================

[globals]
RECORD_5003=all
VM_5003=Off

RECORD_10000=noo
VM_10000=Off

RECORD_5000=noo
VM_5000=Off

RECORD_555=noo
VM_555=On

RECORD_3001=noo
VM_3001=Off


RECORD_2121=all
VM_2121=Off






; BEGIN RCM QUEUE GUI GLOBALS
QUEUE_7772=7772
QUEUE_7441=7441
QUEUE_7008=7008
; END RCM QUEUE GUI GLOBALS
RECORD_5858=noo
VM_5858=Off


RECORD_9999=noo
VM_9999=Off


RECORD_5004=noo
VM_5004=Off


RECORD_5002=noo
VM_5002=Off

RECORD_5001=noo
VM_5001=Off



[internal]
exten => 5003,1,Goto(dexter,${EXTEN},1)
exten => 10000,1,Goto(dexter,${EXTEN},1)
exten => 5000,1,Goto(dexter,${EXTEN},1)
exten => 555,1,Goto(dexter,${EXTEN},1)
exten => 3001,1,Goto(dexter,${EXTEN},1)
exten => 2121,1,Goto(dexter,${EXTEN},1)
exten => 9632,1,Goto(rcm-paging-intercom,${EXTEN},1)
exten => 258,1,Goto(rcm-paging-intercom,${EXTEN},1)
exten => 3698,1,Goto(rcm-paging-intercom,${EXTEN},1)

; BEGIN RCM QUEUE GUI INTERNAL
include => rcm-queue-routes
include => rcm-queue-features
include => speed-dials
; END RCM QUEUE GUI INTERNAL
exten => 5858,1,Goto(dexter,${EXTEN},1)
exten => 9999,1,Goto(dexter,${EXTEN},1)
exten => 5004,1,Goto(dexter,${EXTEN},1)
exten => 5002,1,Goto(dexter,${EXTEN},1)
exten => 5001,1,Goto(dexter,${EXTEN},1)


exten => 9990,1,Goto(ivr-9990,9990,1)
exten => 9900,1,Goto(ann-9900,9900,1)
exten => 8888,1,Goto(ann-8888,8888,1)
exten => _7XXX,1,Goto(rcm-ring-groups,${EXTEN},1)
exten => _8XXX,1,Goto(rcm-ring-groups,${EXTEN},1)

exten => 7777,1,Goto(ivr-7777,7777,1)
exten => 8877,1,Goto(queue,8877,1)
exten => _*71X.,1,Goto(queue-feature-codes,${EXTEN},1)
exten => _*72X.,1,Goto(queue-feature-codes,${EXTEN},1)
exten => _*73X.,1,Goto(queue-feature-codes,${EXTEN},1)
exten => _*74X.,1,Goto(queue-feature-codes,${EXTEN},1)
exten => 7505,1,Goto(ivr-7505,7505,1)
exten => 8523,1,Goto(ivr-8523,8523,1)
exten => 7455,1,Goto(ivr-7455,7455,1)
[ivr-9990]
exten => 9990,1,Answer()
 same => n,Mset(test=1,cuc=1)
 same => n(start),Background(rcm/ivr/ivr_9990_welcome)
 same => n,WaitExten(10)

exten => 1,1,Goto(internal,2222,1)
exten => 2,1,Goto(internal,7001,1)

exten => i,1,Set(test=$[${test} + 1])
 same => n,GotoIf($[${test} < 4]?9990,start)
 same => n,Goto(internal,2222,1)
exten => t,1,Set(cuc=$[${cuc} + 1])
 same => n,GotoIf($[${cuc} < 4]?9990,start)
 same => n,Goto(internal,2222,1)

; --- ANN GUI: test_ann ---
[ann-9900]
exten => 9900,1,Answer()
 same => n,Playback(rcm/ann/ann_9900)
 same => n,Queue(support)

; --- ANN GUI: rwa ---

; --- ANN GUI: rwa ---
[ann-8888]
exten => 8888,1,Answer()
 same => n,Playback(rcm/ann/ann_8888)
 same => n,Hangup()


; --- IVR GUI: ivr-7505 ---
[rcm-ring-groups]
; AUTOGENERATED from /etc/asterisk/rcm_ring_groups.json
; Generated: 2026-03-05T11:30:45+02:00

exten => 8001,1,NoOp(RCM-RG id=7001 name=support strategy=ringall)
 same => n,Dial(PJSIP/2121&PJSIP/2222,20,Tt)
 same => n,Hangup()

exten => 7002,1,NoOp(RCM-RG id=7002 name=sales strategy=ordered)
 same => n,Dial(PJSIP/2233,10,Tt)
 same => n,GotoIf(0""="ANSWER"]?done)
 same => n,Dial(PJSIP/2211,10,Tt)
 same => n,GotoIf(0""="ANSWER"]?done)
 same => n(done),Hangup()

; RCM-RG-END


; --- IVR GUI: yeye ---
[ivr-7777]
exten => 7777,1,Answer()
 same => n,Mset(test=1,cuc=1)
 same => n(start),Background(rcm/ivr/ivr_7777_welcome)
 same => n,WaitExten(10)

exten => 1,1,Goto(internal,2222,1)
exten => 2,1,Goto(internal,7001,1)

exten => i,1,Set(test=$[${test} + 1])
 same => n,GotoIf($[${test} < 4]?7777,start)
 same => n,Goto(internal,2222,1)
exten => t,1,Set(cuc=$[${cuc} + 1])
 same => n,GotoIf($[${cuc} < 4]?7777,start)
 same => n,Goto(internal,2222,1)
; --- RCM QUEUE SHARED BEGIN ---
[queue-feature-codes]
exten => _*71X.,1,Answer()
 same => n,Set(QNUM=${EXTEN:3})
 same => n,Set(MEMBERIFACE=PJSIP/${CALLERID(num)})
 same => n,AddQueueMember(${QNUM},${MEMBERIFACE})
 same => n,GotoIf($["${AQMSTATUS}"="MEMBERALREADY"]?already,1)
 same => n,Playback(agent-loginok)
 same => n,SayDigits(${QNUM})
 same => n,Hangup()

exten => already,1,Playback(agent-alreadyon)
 same => n,SayDigits(${QNUM})
 same => n,Hangup()

exten => _*72X.,1,Answer()
 same => n,Set(QNUM=${EXTEN:3})
 same => n,Set(MEMBERIFACE=PJSIP/${CALLERID(num)})
 same => n,RemoveQueueMember(${QNUM},${MEMBERIFACE})
 same => n,GotoIf($["${RQMSTATUS}"!="REMOVED"]?notinqueue,1)
 same => n,Playback(agent-loggedoff)
 same => n,SayDigits(${QNUM})
 same => n,Hangup()

exten => notinqueue,1,Playback(agent-notonqueue)
 same => n,SayDigits(${QNUM})
 same => n,Hangup()

exten => _*73X.,1,Answer()
 same => n,Set(QNUM=${EXTEN:3})
 same => n,Set(MEMBERIFACE=PJSIP/${CALLERID(num)})
 same => n,PauseQueueMember(${QNUM},${MEMBERIFACE},,break)
 same => n,GotoIf($["${PQMSTATUS}"="PAUSED"]?ok:fail)
 same => n(ok),Playback(agent_pause_successfully_in_queue)
 same => n,Hangup()
 same => n(fail),Playback(sorry-cant-let-you-do-that)
 same => n,Hangup()

exten => _*74X.,1,Answer()
 same => n,Set(QNUM=${EXTEN:3})
 same => n,Set(MEMBERIFACE=PJSIP/${CALLERID(num)})
 same => n,UnpauseQueueMember(${QNUM},${MEMBERIFACE},,back)
 same => n,GotoIf($["${UPQMSTATUS}"="UNPAUSED"]?ok:notin)
 same => n(ok),Playback(agent_unpause_and_back_to_queue_successfully.)
 same => n,Hangup()
 same => n(notin),Playback(This_extension_is_not_in_this_queue)
 same => n,Hangup()

; BEGIN RCM QUEUE GUI CONTEXTS
[queue-7772]
exten => 7772,1,Answer()
 same => n,Queue(7772,tT,,,20)
 same => n,Hangup()

[queue-7441]
exten => 7441,1,Answer()
 same => n,Queue(7441,tT,,,20)
 same => n,Hangup()

[queue-7008]
exten => 7008,1,Answer()
 same => n,Queue(7008,tT,,,20)
 same => n,Hangup()
;////////////////////////////////////////////////////////////////////////////////////////////////////////
[test-context-123]
exten => 9999,1,Hangup()
;////////////////////////////////////////////////////////////////////////////////////////////////////////
[rcm-queue-routes]
exten => 7772,1,Goto(queue-7772,7772,1)
exten => 7441,1,Goto(queue-7441,7441,1)
exten => 7008,1,Goto(queue-7008,7008,1)

[rcm-queue-features]
exten => _*71XXXX,1,Answer()
 same => n,Set(QNUM=${EXTEN:3})
 same => n,Set(QUEUENAME=${GLOBAL(QUEUE_${QNUM})})
 same => n,GotoIf($["${QUEUENAME}"=""]?bad,1)
 same => n,Set(MEMBERIFACE=PJSIP/${CALLERID(num)})
 same => n,AddQueueMember(${QUEUENAME},${MEMBERIFACE})
 same => n,Playback(agent-loginok)
 same => n,SayDigits(${QNUM})
 same => n,Hangup()

exten => bad,1,Playback(invalid)
 same => n,Hangup()

exten => _*72XXXX,1,Answer()
 same => n,Set(QNUM=${EXTEN:3})
 same => n,Set(QUEUENAME=${GLOBAL(QUEUE_${QNUM})})
 same => n,GotoIf($["${QUEUENAME}"=""]?bad2,1)
 same => n,Set(MEMBERIFACE=PJSIP/${CALLERID(num)})
 same => n,RemoveQueueMember(${QUEUENAME},${MEMBERIFACE})
 same => n,Playback(agent-loggedoff)
 same => n,SayDigits(${QNUM})
 same => n,Hangup()

exten => bad2,1,Playback(invalid)
 same => n,Hangup()

exten => _*73XXXX,1,Answer()
 same => n,Set(QNUM=${EXTEN:3})
 same => n,Set(QUEUENAME=${GLOBAL(QUEUE_${QNUM})})
 same => n,GotoIf($["${QUEUENAME}"=""]?bad3,1)
 same => n,Set(MEMBERIFACE=PJSIP/${CALLERID(num)})
 same => n,PauseQueueMember(${QUEUENAME},${MEMBERIFACE},,break)
 same => n,Playback(agent-loginok)
 same => n,Hangup()

exten => bad3,1,Playback(invalid)
 same => n,Hangup()

exten => _*74XXXX,1,Answer()
 same => n,Set(QNUM=${EXTEN:3})
 same => n,Set(QUEUENAME=${GLOBAL(QUEUE_${QNUM})})
 same => n,GotoIf($["${QUEUENAME}"=""]?bad4,1)
 same => n,Set(MEMBERIFACE=PJSIP/${CALLERID(num)})
 same => n,UnpauseQueueMember(${QUEUENAME},${MEMBERIFACE},,back)
 same => n,Playback(agent-loginok)
 same => n,Hangup()

exten => bad4,1,Playback(invalid)
 same => n,Hangup()



[rcm-paging-intercom]

; test (paging)
exten => 3698,1,NoOp(RCM paging for test)
 same => n,GotoIf($["2121" = "${CALLERID(num)}"]?allow)
 same => n,Hangup()
 same => n(allow),NoOp(Caller allowed)
 same => n,Page(PJSIP/555,A(rcm/media/general/para))
 same => n,Hangup()

; rrrr (intercom)
exten => 258,1,NoOp(RCM intercom for rrrr)
 same => n,GotoIf($["555" = "${CALLERID(num)}"]?allow)
 same => n,GotoIf($["2121" = "${CALLERID(num)}"]?allow)
 same => n,Hangup()
 same => n(allow),NoOp(Caller allowed)
 same => n,Page(PJSIP/2121&PJSIP/555,dA(rcm/media/general/para))
 same => n,Hangup()

; ttttooo (paging)
exten => 9632,1,NoOp(RCM paging for ttttooo)
 same => n,GotoIf($["555" = "${CALLERID(num)}"]?allow)
 same => n,GotoIf($["2121" = "${CALLERID(num)}"]?allow)
 same => n,GotoIf($["6363" = "${CALLERID(num)}"]?allow)
 same => n,Hangup()
 same => n(allow),NoOp(Caller allowed)
 same => n,Page(PJSIP/555&PJSIP/2121&PJSIP/5001)
 same => n,Hangup()


; --- IVR GUI: welcome ---


; --- IVR GUI: welcome ---


; --- IVR GUI: welcome ---
[ivr-7505]
exten => 7505,1,Answer()
 same => n,Mset(test=1,cuc=1)
 same => n(start),Background(rcm/ivr/ivr_7505_welcome)
 same => n,WaitExten(10)

exten => 1,1,Goto(internal,2222,1)
exten => 2,1,Goto(internal,7001,1)

exten => i,1,Set(test=$[${test} + 1])
 same => n,GotoIf($[${test} < 4]?7505,start)
 same => n,Playback(goodbye)
 same => n,Hangup()
exten => t,1,Set(cuc=$[${cuc} + 1])
 same => n,GotoIf($[${cuc} < 4]?7505,start)
 same => n,Playback(goodbye)
 same => n,Hangup()

[pickup-2]
name=ccc
group=2
members=555,2121,5000,5001,5002
; --- PICKUP GROUP GUI: ccc ---
[pickup-2]
name=ccc
group=2
members=555,2121,5000,5001,5002
[pickup-3]
name=fff
group=3
members=555,5001


; --- IVR GUI: wawa ---
[ivr-8523]
exten => 8523,1,Answer()
 same => n,Mset(test=1,cuc=1)
 same => n(start),Background(rcm/ivr/ivr_8523_welcome)
 same => n,WaitExten(10)

exten => 1,1,Goto(internal,555,1)
exten => 2,1,Goto(internal,555,1)

exten => i,1,Set(test=$[${test} + 1])
 same => n,GotoIf($[${test} < 4]?8523,start)
 same => n,Goto(internal,2222,1)
exten => t,1,Set(cuc=$[${cuc} + 1])
 same => n,GotoIf($[${cuc} < 4]?8523,start)
 same => n,Goto(internal,2222,1)


; --- IVR GUI: welcome ---
[ivr-7455]
exten => 7455,1,Answer()
 same => n,Mset(test=1,cuc=1)
	 same => n(start),Background(rcm/ivr/ivr_7455_welcome)
 same => n,WaitExten(10)

exten => 1,1,Goto(internal,555,1)
exten => 2,1,Goto(internal,555,1)

exten => i,1,Set(test=$[${test} + 1])
 same => n,GotoIf($[${test} < 4]?7455,start)
 same => n,Goto(internal,2222,1)
exten => t,1,Set(cuc=$[${cuc} + 1])
 same => n,GotoIf($[${cuc} < 4]?7455,start)
 same => n,Goto(internal,2222,1)

[speed-dials]

exten => 23,1,Goto(ivr-7455,7455,1)
exten => 3,1,Goto(ivr-9990,9990,1)
